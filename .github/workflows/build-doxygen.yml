name: Build C++ Doxygen documentation
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y doxygen
      - run: doxygen doc/config/Doxyfile.libMagAOX
